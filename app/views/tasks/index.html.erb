<h1>Your Tasks</h1>

  <% @tasks.each do |task|  %>
  <h2><%= link_to task.name, task_path(task.id) %> </h2>
   </a>
   <%= link_to "Edit", edit_task_path(task.id) %>
   <%= link_to "Delete", destroy_task_path(task.id) %>
   <% if !task.completiondate %>
  <%= link_to 'Complete Task', complete_task_path(task.id), method: :patch %>
    <% else %>
   <%= link_to 'Undo  Complete', undo_complete_path(task.id), method: :patch %> 
    <% end %>
  <% end %>
